
// noinspection JSUnusedLocalSymbols

let initialState = {};

type ActionType = {
    type: string
}

const sidebarReducer = (state = initialState, action: ActionType): typeof initialState=> {
    return state;
};

export default sidebarReducer;
